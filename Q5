#include <iostream>
#include <vector>
using namespace std;
int main(){
    int n; if(!(cin>>n)) return 0;
    vector<int> d(n);
    for(int i=0;i<n;i++) cin>>d[i];
    for(int i=0;i<n;i++){ for(int j=0;j<n;j++) cout << (i==j ? d[i] : 0) << " "; cout << "\n"; }
    return 0;
}
